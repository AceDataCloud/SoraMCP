apiVersion: v1
kind: Service
metadata:
  labels:
    app: mcp-sora
  name: mcp-sora
  namespace: acedatacloud
spec:
  ports:
    - name: http
      port: 8000
      targetPort: 8000
  selector:
    app: mcp-sora
